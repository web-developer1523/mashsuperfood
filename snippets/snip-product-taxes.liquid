{% comment %}
    Renders product tax information.
    Accepts:
    - cart: {Object} Cart global liquid object (optional)
    - shop: {Object} Shop global liquid object (optional)

    Usage:
    {% render 'snip-product-taxes' %}
{% endcomment %}
{%- if cart.taxes_included or shop.shipping_policy.body != blank -%}
  <div class="product__policies rte mb2" data-product-policies>
    {%- if cart.taxes_included -%}
      {{ 'products.product.include_taxes' | t }}
    {%- endif -%}
    {%- if shop.shipping_policy.body != blank -%}
      {{ 'products.product.shipping_policy_html' | t: link: shop.shipping_policy.url }}
    {%- endif -%}
  </div>
{%- endif -%}